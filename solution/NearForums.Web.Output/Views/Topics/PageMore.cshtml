@model Topic
@{
    Layout = null;
}
@{
	if (Model.Messages.Count > 0)
	{
		foreach (Message m in Model.Messages)
		{
			Html.RenderPartial("MessageItem", m, ViewData.Append(new { HasPostAccess = Model.HasPostAccess(Role) }));
		}
	}
}